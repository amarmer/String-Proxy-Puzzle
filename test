// No headers

template <typename T> 
constexpr auto StringProxy()
{
    struct proxy {
        constexpr bool operator==(T&&) const {
            return ???;
        }
    };
    return proxy { };
}

static_assert(StringProxy<const char(&)[12]>() == "StringProxy", "");
                                                           
int main() { return 0; }
